---
sidebar_position: 3
sidebar_label: <a> Anchor
---

import CodeEditor from "@site/src/components/CodeEditor"
import { anchorExample1, anchorExample2 } from "@site/src/data/examples"

# &lt;a&gt; - Anchor
Elemen anchor adalah elemen yang dapat digunakan untuk membuat link ke situs lain di internet (termasuk situs yang sama dengan page yang sedang dikunjungi). Elemen anchor dapat didefinisikan dengan menggunakan tag `<a></a>`

Link yang ingin dituju oleh elemen anchor dapat didefinisikan dengan memberikan atribut href ke anchor tag yang nilainya berupa string URL.

#### Daftar Atribut Anchor Tag:
- `href`: URL yang ingin dituju
- `target`: Target window dimana halaman akan dibuka. Dapat memiliki nilai sebagai berikut:
  - `_self`: Buka link pada halaman yang sama (default).
  - `_blank`: Buka link pada halaman baru.
  - `_parent`: Buka link pada halaman *parent* (khusus untuk iframe).


### Example 1: Text content as link
Berikut ini adalah contoh penggunaan anchor tag pada sebuah halaman HTML:
<CodeEditor defaultValue={anchorExample1} />

### Example 2: Non-text content as link
Selain untuk teks, anchor tag juga dapat digunakan untuk mengubah elemen non-teks seperti gambar dan button menjadi link:
<CodeEditor defaultValue={anchorExample2} />
 
