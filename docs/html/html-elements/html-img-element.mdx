---
sidebar_position: 4
sidebar_label: <img> Image
title: <img> Image
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

import CodeEditor from "@site/src/components/CodeEditor"
import { imgExample } from "@site/src/data/examples"

# &lt;img&gt; - Image
Elemen image adalah elemen yang dapat digunakan untuk menampilkan gambar di sebuah halaman web. Elemen image dapat didefinisikan dengan menggunakan tag `<img />`.

Untuk memberi tahu browser image yang harus ditampilkan, kalian harus mendefinisikan value dari atribut `src` pada image tag. Selain itu terdapat pula beberapa atribut lain yang mengatur properti dari gambar seperti yang dituliskan pada bagian selanjutnya.

### Daftar Atribut Image Tag
- `src`: URL yang ingin dituju
- `alt`: Deskripsi dari image yang ditampilkan (digunakan ketika image tidak dapat di-load dan untuk accessibility)
- `height`: Set height dari image yang ingin ditampilkan (sebaiknya menggunakan CSS saja)
- `width`: Set width dati image yang ingin ditampilkan (sebaiknya menggunakan CSS saja)

### Contoh Penggunaan
<Tabs>
    <TabItem value="example1" label="Menampilkan Gambar" default>
        <CodeEditor defaultValue={imgExample} />
    </TabItem>
</Tabs>
 
